import React from 'react';
import { Layout } from 'antd';

const { Header } = Layout;

function AppHeader() {
  return (
    <Layout>
      <Header>header</Header>
    </Layout>
  );
}

export default AppHeader;
